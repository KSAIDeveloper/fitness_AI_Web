# Fitness Calorie Calculator (Fito) - Vite Edition

운동 칼로리 소모량을 계산하는 React + Vite 웹 애플리케이션입니다.

## 🏗️ 프로젝트 구조

```
src/
├── components/
│   ├── layout/              # 레이아웃 컴포넌트
│   │   ├── Header.jsx       # 상단 헤더
│   │   ├── Header.css
│   │   ├── Footer.jsx       # 하단 푸터
│   │   ├── Footer.css
│   │   ├── MainLayout.jsx   # 메인 레이아웃 (Header + Content + Footer)
│   │   └── MainLayout.css
│   │
│   └── pages/               # 페이지 컴포넌트
│       ├── Home.jsx         # 홈페이지
│       ├── Home.css
│       ├── CalorieCalculator.jsx  # 칼로리 계산기 페이지
│       └── CalorieCalculator.css
│
├── App.jsx                  # 라우터 설정
├── App.css
├── main.jsx                 # 진입점 (Vite는 main.jsx 사용)
└── index.css                # 전역 스타일
```

## 🚀 시작하기

### 1. 패키지 설치

```bash
npm install
```

### 2. 개발 서버 실행 (중요!)

```bash
npm run dev
```

**주의:** Vite 프로젝트는 `npm start`가 아니라 **`npm run dev`**를 사용합니다!

브라우저에서 표시되는 주소(보통 http://localhost:5173)로 접속하세요.

### 3. 빌드

```bash
npm run build
```

빌드된 파일은 `dist/` 폴더에 생성됩니다.

### 4. 빌드 미리보기

```bash
npm run preview
```

## 📱 페이지 구조

### 라우트
- `/` - 홈페이지 (Home.jsx)
- `/calculator` - 칼로리 계산기 (CalorieCalculator.jsx)

### MainLayout
- Header와 Footer는 모든 페이지에서 공유됩니다
- `react-router-dom`의 `Outlet`을 사용하여 자식 페이지를 렌더링합니다

## 🎨 주요 컴포넌트

### Header
- 로고, 네비게이션 메뉴, CTA 버튼
- 스티키 헤더 (스크롤 시 상단 고정)

### Footer
- 회사 정보, 링크, 법적 고지
- 4개 섹션의 그리드 레이아웃

### Home
- 히어로 섹션: 메인 타이틀과 CTA 버튼
- 기능 소개: 4개의 주요 기능 카드
- 행동 유도 섹션

### CalorieCalculator
- 무게 입력 (kg/lbs 전환)
- 운동 시간 선택 (프리셋 + 커스텀)
- 운동 유형 선택
- 실시간 칼로리 계산
- MET 기반 계산 상세 정보

## 🛠️ 주요 기능

1. **React Router 기반 라우팅**
   - SPA(Single Page Application) 구조
   - Header/Footer 공유로 효율적인 레이아웃

2. **반응형 디자인**
   - 모바일, 태블릿, 데스크톱 대응
   - CSS Grid와 Flexbox 활용

3. **MET 기반 칼로리 계산**
   - 과학적 계산 방식
   - 실시간 업데이트
   - 다양한 운동 유형 지원

## 📦 사용된 패키지

- `react` ^19.1.1 - UI 라이브러리
- `react-dom` ^19.1.1 - React DOM 렌더링
- `react-router-dom` - 라우팅
- `vite` - 빌드 도구 (빠른 개발 서버)

## 🎯 새 페이지 추가하기

1. `src/components/pages/` 에 새 컴포넌트 생성

```jsx
// src/components/pages/NewPage.jsx
import React from 'react';
import './NewPage.css';

const NewPage = () => {
  return (
    <div className="new-page">
      <h1>새 페이지</h1>
    </div>
  );
};

export default NewPage;
```

2. `src/App.jsx`에 라우트 추가

```jsx
import NewPage from './components/pages/NewPage';

// Routes 안에 추가:
<Route path="new-page" element={<NewPage />} />
```

3. 필요시 Header에 네비게이션 링크 추가

## 💡 Vite vs Create React App 차이점

| 항목 | Create React App | Vite |
|------|-----------------|------|
| 개발 서버 실행 | `npm start` | `npm run dev` |
| 진입 파일 | `src/index.js` | `src/main.jsx` |
| 빌드 속도 | 느림 | 매우 빠름 |
| HMR | 있음 | 더 빠른 HMR |

## 🔧 자주 묻는 질문

**Q: `npm start`가 안 돼요!**
A: Vite는 `npm run dev`를 사용합니다!

**Q: 파일 변경이 반영이 안 돼요**
A: Vite의 HMR이 매우 빠르므로 자동으로 반영됩니다. 브라우저를 확인해보세요.

**Q: 빌드 파일은 어디 있나요?**
A: `npm run build` 실행 후 `dist/` 폴더에 생성됩니다.

## 📄 라이선스

MIT
